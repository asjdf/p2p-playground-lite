# Security Guide

本文档说明 P2P Playground Lite 的安全功能及使用方法。

## 概述

P2P Playground Lite 实现了以下安全机制：

1. **应用包签名验证** - 使用 Ed25519 数字签名确保应用包来源可信和完整性
2. **默认强制签名策略** - ⚠️ **默认拒绝未签名包**（可配置为允许）
3. **多密钥信任模型** - 支持多个可信公钥，便于密钥轮换和多团队部署

**⚠️ 重要变更（2026-01-19）**：
- 从此版本开始，**默认要求所有应用包必须签名**
- 未签名的包将被拒绝部署
- 开发/测试环境可通过配置显式关闭此限制

## 应用包签名

### 密钥生成

首先在 controller 端生成密钥对：

```bash
# 生成密钥对（默认保存到 ~/.p2p-playground/keys/）
controller keygen

# 指定输出目录
controller keygen -o /path/to/keys
```

输出示例：
```
Generating Ed25519 key pair...

✓ Key pair generated successfully!
  Private key: /path/to/keys/controller.key
  Public key:  /path/to/keys/controller.pub

⚠️  Keep the private key secure and never share it.
📤 Distribute the public key to nodes for signature verification.

Public key (hex): 58231049e8615ab913ce3671f75c7141a9e4759682cfa2b8148e2eb5422d6965
```

**重要**：
- 私钥 (`controller.key`) 必须妥善保管，不能泄露
- 公钥 (`controller.pub`) 需要分发到所有 daemon 节点

### 签名应用包

在部署前对应用包进行签名：

```bash
# 签名应用包
controller sign myapp-1.0.0.tar.gz

# 使用指定的私钥
controller sign myapp-1.0.0.tar.gz --key /path/to/controller.key
```

这会生成签名文件 `myapp-1.0.0.tar.gz.sig`。

输出示例：
```
Signing package: myapp-1.0.0.tar.gz

✓ Package signed successfully!
  Signature: myapp-1.0.0.tar.gz.sig
  Signature (hex): 3a7b2c...

You can now deploy this package with signature verification.
```

### 部署签名包

部署时，controller 会自动检测并包含签名文件：

```bash
# deploy 命令自动查找 .sig 文件
controller deploy myapp-1.0.0.tar.gz
```

如果存在 `myapp-1.0.0.tar.gz.sig`，签名会自动包含在部署请求中。

## Controller 配置

### 配置文件支持

Controller 支持通过配置文件统一管理设置（类似 kubectl）：

```bash
# 使用默认配置文件 (~/.p2p-playground/controller.yaml)
controller deploy app.tar.gz

# 指定配置文件
controller --config /path/to/config.yaml deploy app.tar.gz
controller -c prod-config.yaml nodes
```

### Controller 配置示例

```yaml
# ~/.p2p-playground/controller.yaml
node:
  listen_addrs:
    - /ip4/0.0.0.0/tcp/9001
  enable_mdns: true

logging:
  level: info      # debug|info|warn|error
  format: console  # console|json

storage:
  data_dir: ~/.p2p-playground-controller
  keys_dir: ~/.p2p-playground-controller/keys

security:
  allow_unsigned_packages: false  # 拒绝未签名包（推荐）

deployment:
  timeout: 5m
  retry_attempts: 3
  retry_delay: 10s
```

**配置加载顺序**：
1. 如果指定 `--config` 参数，使用该配置文件
2. 否则检查默认位置 `~/.p2p-playground/controller.yaml`
3. 如果默认文件不存在，使用硬编码默认值

## 节点配置

### 配置可信公钥

在 daemon 节点上配置可信的公钥：

1. 创建可信公钥目录：
```bash
mkdir -p ~/.p2p-playground/keys/trusted
```

2. 将 controller 的公钥复制到该目录：
```bash
cp controller.pub ~/.p2p-playground/keys/trusted/
```

daemon 会自动加载该目录下所有 `.pub` 文件作为可信公钥。

### 配置签名验证策略

在 daemon 配置文件中设置安全选项：

```yaml
# daemon.yaml
security:
  # 是否允许部署未签名的应用包
  allow_unsigned_packages: false  # ⚠️ 默认 false（拒绝未签名包）

  # 可信公钥目录（可选，默认为 keys/trusted）
  public_keys_dir: ~/.p2p-playground/keys/trusted
```

**配置选项说明**：

- `allow_unsigned_packages: false` (**默认值，推荐**)
  - 必须有签名才能部署
  - 签名验证失败会拒绝部署
  - 未签名的包会被拒绝
  - 适合生产环境和安全敏感场景
  - **这是默认行为，更安全**

- `allow_unsigned_packages: true` (开发/测试环境)
  - 如果有签名，会进行验证
  - 如果没有签名，会记录 warning 但仍然允许部署
  - 适合本地开发和测试环境
  - **需要显式配置才能允许未签名包**

## 签名验证流程

当 daemon 收到部署请求时：

1. **检查签名是否存在**
   - 如果有签名 → 进入验证流程
   - 如果没有签名：
     - 如果 `allow_unsigned_packages: false`（默认） → 拒绝部署
     - 如果 `allow_unsigned_packages: true` → 记录 warning，允许部署

2. **签名验证**（如果有签名）
   - 计算应用包的 SHA-256 哈希
   - 使用可信公钥目录中的所有公钥尝试验证
   - 任何一个公钥验证成功 → 验证通过
   - 所有公钥都验证失败 → 拒绝部署

3. **验证结果**
   - 验证通过 → 继续部署流程
   - 验证失败 → 拒绝部署，返回错误

## 日志示例

### Controller 日志

```
# 有签名的部署
2026-01-19T10:00:00Z  info  package signature found  sig_path=/path/to/package.tar.gz.sig
2026-01-19T10:00:01Z  info  sending package  file=package.tar.gz  size=1425653

# 无签名的部署
2026-01-19T10:00:00Z  warn  no package signature found, deploying without signature verification
```

### Daemon 日志

```
# 签名验证成功
2026-01-19T10:00:02Z  info  verifying package signature
2026-01-19T10:00:02Z  info  signature verified  public_key=controller.pub
2026-01-19T10:00:02Z  info  package deployed  app_id=myapp-1.0.0

# 签名验证失败
2026-01-19T10:00:02Z  info  verifying package signature
2026-01-19T10:00:02Z  error  signature verification failed  error=invalid signature
# 部署被拒绝

# 无签名但不强制
2026-01-19T10:00:02Z  warn  package deployed without signature verification
```

## 最佳实践

### 开发环境

1. 生成测试密钥对
2. 签名应用包（可选）
3. 配置 `allow_unsigned_packages: true` （允许未签名包，便于开发）
4. 观察日志确认签名机制工作正常

### 生产环境

1. 生成正式密钥对，妥善保管私钥
2. 将公钥分发到所有生产节点
3. 所有应用包必须签名
4. 保持 `allow_unsigned_packages: false`（默认，拒绝未签名包）
5. 定期轮换密钥对

### 密钥管理

- 私钥权限设置为 `0600`（仅所有者可读写）
- 定期备份私钥（加密存储）
- 使用 HSM 或密钥管理服务存储私钥（可选）
- 每个环境使用不同的密钥对
- 定期轮换密钥（建议每年一次）

### 多公钥支持

daemon 支持同时信任多个公钥，这在以下场景很有用：

1. **密钥轮换期间** - 同时信任旧密钥和新密钥
2. **多团队发布** - 每个团队有自己的密钥对
3. **多环境** - 不同环境的包可以用不同密钥签名

只需将多个 `.pub` 文件放到 `keys/trusted/` 目录即可。

## 安全决策记录

### Phase 2.1 实现决策

**决策** (⚠️ 已更新)：签名验证默认强制（`allow_unsigned_packages: false`）

**理由**：
1. **安全优先** - 默认拒绝未签名包，防止未授权代码执行
2. **清晰的安全姿态** - 用户必须显式选择允许未签名包
3. **符合行业最佳实践** - 与容器签名、代码签名标准一致
4. **更好的配置名称** - `allow_unsigned_packages` 语义更清晰（false = 更安全）

**配置迁移**：
- 旧版本使用 `require_signed_packages: true/false`
- 新版本使用 `allow_unsigned_packages: true/false`
- 逻辑反转：`require_signed: true` = `allow_unsigned: false`

**未来计划**：
- Phase 3：添加更细粒度的策略（按应用、按节点标签等）
- Phase 3：密钥撤销和轮换自动化

### 签名算法选择

**选择**：Ed25519

**理由**：
- 性能优秀（签名和验证都很快）
- 密钥尺寸小（32字节公钥，64字节私钥）
- 安全性高（128位安全级别）
- Go 标准库原生支持
- 现代密码学标准

## 故障排查

### 签名验证失败

**错误**：`signature verification failed: invalid signature`

**可能原因**：
1. 签名文件损坏
2. 应用包被篡改
3. 使用了错误的公钥
4. 公钥文件格式不正确

**解决方法**：
1. 重新签名应用包
2. 确认公钥正确复制到 daemon
3. 检查公钥文件权限和格式
4. 查看 daemon 日志确认加载了哪些公钥

### 没有可信公钥

**错误**：`no trusted public keys found in /path/to/keys/trusted`

**解决方法**：
1. 创建 `keys/trusted` 目录
2. 复制公钥文件到该目录
3. 确保公钥文件扩展名为 `.pub`
4. 重启 daemon

### 找不到签名文件

**警告**：`no package signature found, deploying without signature verification`

**说明**：这不是错误，只是提示没有找到签名文件。

**解决方法**（如果需要签名）：
1. 使用 `controller sign` 对包进行签名
2. 确保 `.sig` 文件和包文件在同一目录
3. 重新部署

## 未来增强

以下功能计划在后续版本中实现：

1. **PSK 节点认证** - 使用预共享密钥进行节点级别的认证
2. **证书认证** - 支持 X.509 证书进行节点认证
3. **签名时间戳** - 记录签名时间，防止重放攻击
4. **撤销列表** - 支持撤销已泄露的密钥
5. **细粒度策略** - 按应用、标签等维度配置不同的安全策略
